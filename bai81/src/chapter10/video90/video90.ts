export { };
console.log("video 90");

const name: string = "duong";
name.toUpperCase();

const name1: any = "duong";
name1.toUpperCase();

const name2: unknown = "duong";
if (typeof (name2) === "string") {
    name2.toUpperCase();
}

